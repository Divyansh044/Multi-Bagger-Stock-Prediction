# ─────────────────────────────────────────────
#  Multibagger Stock Prediction System
#  requirements.txt
# ─────────────────────────────────────────────

# ── Data Collection ───────────────────────────
yfinance>=0.2.36          # Yahoo Finance API wrapper (free, no API key)
requests>=2.28.0          # HTTP library for web requests

# ── Data Processing ───────────────────────────
pandas>=1.5.0             # DataFrames, CSV I/O, time-series operations
numpy>=1.23.0             # Numerical arrays and math operations
pyarrow>=11.0.0           # Fast Parquet file read/write (for large data)

# ── Technical Analysis / Feature Engineering ──
ta>=0.10.2                # 60+ technical indicators (RSI, MACD, BB, etc.)

# ── Machine Learning ──────────────────────────
scikit-learn>=1.2.0       # Random Forest, preprocessing, metrics
xgboost>=1.7.0            # Gradient Boosted Trees (usually best performer)
imbalanced-learn>=0.10.0  # SMOTE — handle class imbalance (few multibaggers)
joblib>=1.2.0             # Save and load trained model files

# ── Visualization / Dashboard ─────────────────
streamlit>=1.28.0         # Turns Python scripts into web dashboards
plotly>=5.14.0            # Interactive charts (candlesticks, line, bar)
matplotlib>=3.6.0         # Static charts (for model evaluation plots)

# ── Utilities ─────────────────────────────────
tqdm>=4.64.0              # Progress bars for long-running loops
python-dotenv>=1.0.0      # Load secrets from .env file (if needed)
